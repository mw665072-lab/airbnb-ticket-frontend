import { CheckCircle2, Info, TriangleAlert, X } from "lucide-react";
import { useToastStore } from "@/store/toast";
export function ToastViewport() { const { toasts, remove } = useToastStore(); return <div className="fixed bottom-5 right-5 z-[100] flex w-[min(92vw,380px)] flex-col gap-3">{toasts.map((toast) => <div key={toast.id} className="flex items-start gap-3 rounded-2xl border border-slate-200 bg-white p-4 shadow-2xl"><span className={toast.kind === "success" ? "text-emerald-600" : toast.kind === "error" ? "text-rose-600" : "text-blue-600"}>{toast.kind === "success" ? <CheckCircle2 className="h-5 w-5" /> : toast.kind === "error" ? <TriangleAlert className="h-5 w-5" /> : <Info className="h-5 w-5" />}</span><p className="flex-1 text-sm font-semibold text-slate-700">{toast.message}</p><button aria-label="Dismiss notification" onClick={() => remove(toast.id)}><X className="h-4 w-4 text-slate-400" /></button></div>)}</div>; }
