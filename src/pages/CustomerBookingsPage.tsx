import { ArrowRight, Plus } from "lucide-react";
import { useState } from "react";
import { Link } from "react-router-dom";
import { EmptyState, ErrorState, InlineLoader, Pagination } from "@/components/ui";
import { StatusBadge } from "@/components/StatusBadge";
import { useMyRequests } from "@/query/hooks";
import { formatDate, formatMoney } from "@/lib/format";
export function CustomerBookingsPage(){const[page,setPage]=useState(1);const query=useMyRequests({page,limit:12,sort:"-createdAt"});const requests=query.data?.data.requests||[];return <main className="page-shell py-12"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="eyebrow">My travel</p><h1 className="mt-2 font-display text-4xl font-extrabold tracking-[-.04em] text-slate-900">Booking requests</h1><p className="mt-2 text-sm text-slate-500">Track quotes, payments, conversations, and ticketing.</p></div><Link to="/search" className="btn-primary"><Plus className="h-4 w-4"/>New flight search</Link></div><div className="mt-8">{query.isPending?<InlineLoader/>:query.isError?<ErrorState error={query.error} onRetry={()=>query.refetch()}/>:requests.length?<><div className="grid gap-5 md:grid-cols-2 xl:grid-cols-3">{requests.map(request=><Link key={request.reference} to={`/account/bookings/${request.reference}`} className="surface group p-6 transition hover:-translate-y-1 hover:shadow-xl"><div className="flex items-start justify-between gap-3"><div><p className="text-[10px] font-bold uppercase tracking-[.16em] text-slate-400">{request.reference}</p><h2 className="mt-2 font-display text-xl font-extrabold text-slate-900">{request.route||"Flight request"}</h2></div><StatusBadge status={request.status}/></div><div className="mt-6 grid grid-cols-2 gap-4 rounded-2xl bg-slate-50 p-4"><div><span className="text-[10px] font-bold uppercase text-slate-400">Departure</span><strong className="mt-1 block text-sm text-slate-800">{formatDate(request.departureDate)}</strong></div><div><span className="text-[10px] font-bold uppercase text-slate-400">Quote</span><strong className="mt-1 block text-sm text-slate-800">{request.quote?formatMoney(request.quote.amount,request.quote.currency):"Pending"}</strong></div></div><div className="mt-5 flex items-center justify-between text-xs font-semibold text-slate-400"><span>Created {formatDate(request.createdAt)}</span><ArrowRight className="h-4 w-4 text-blue-800 transition group-hover:translate-x-1"/></div></Link>)}</div><Pagination page={page} totalPages={query.data?.meta?.totalPages||1} onPage={setPage}/></>:<EmptyState title="No booking requests yet" description="Search available flights and submit your first request." action={<Link className="btn-primary" to="/search">Search flights</Link>}/>}</div></main>;}
